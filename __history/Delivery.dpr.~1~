program Delivery;

uses
  System.StartUpCopy,
  FMX.Forms,
  Ind.View.Principal in 'View\Ind.View.Principal.pas' {Form1};

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TForm1, Form1);
  Application.Run;
end.
